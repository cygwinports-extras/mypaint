--- origsrc/mypaint-0.8.0/SConstruct	2010-01-29 05:17:02.000000000 -0600
+++ src/mypaint-0.8.0/SConstruct	2010-01-30 21:59:57.446371000 -0600
@@ -43,10 +43,10 @@ if sys.platform == "win32":
 else:
     # some distros use python2.5-config, others python-config2.5
     try:
-        env.ParseConfig(python + '-config --cflags --ldflags')
+        env.ParseConfig(python + '-config --cflags --libs')
     except OSError:
         print 'going to try python-config instead'
-        env.ParseConfig('python-config --cflags --ldflags')
+        env.ParseConfig('python-config --cflags --libs')
 
 if env.get('CPPDEFINES'):
     # make sure assertions are enabled
